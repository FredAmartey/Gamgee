# Required.
TOGETHER_API_KEY=

# Optional: rate limiting and the free-credit ledger. Both are needed together.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: sign-in. Both are needed together.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Optional: canonical URL and social card host once there is a real domain.
NEXT_PUBLIC_SITE_URL=
